#include "SceneManager.h"

namespace AEC {
	//void SceneManager::CreateScene(const char* sceneName) {
	//	m_scenes[m_idCnt] = Scene(sceneName);
	//	m_idCnt++;
	//}
	//Scene* SceneManager::GetScene(const char* sceneName) {
	//	for (auto& scene : m_scenes) {
	//		if (scene.second.GetName().compare(sceneName))
	//			return &(scene.second);
	//	}
	//	return nullptr;
	//}
	//Scene* SceneManager::GetScene(int id) {
	//	auto it = m_scenes.find(id);
	//	if (it != m_scenes.end()) {
	//		return &(it->second);
	//	}
	//	return nullptr;
	//}
	//Scene* SceneManager::GetActiveScene() {
	//	return m_activeScene;
	//}
}